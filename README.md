# AI 泡沫监测面板

> **仓库状态：正文待恢复。**
>
> 本仓库目前**只有变更历史，没有页面正文**。请勿把它当作可用的监测面板。

## 发生了什么

原仓库 `ourword-ai/ai-bubble-detector` 与其 GitHub Pages 站点（`ourword.ai/ai-bubble-detector/`）
随 GitHub 账号 `ourword-ai` 于 2026 年 8 月被封禁而一并下线。

- 仓库、用户 API、Pages 均返回 404
- 带 token 的 API 调用返回 `403 Sorry. Your account was suspended`
- 域名侧正常（DNS 185.199.108.153、Let's Encrypt 证书有效至 2026-10-28）——故障在 Pages 后端

**`index.html`（约 528KB）没有任何本地副本，git 提交历史一并丢失。**

## 本仓库现有什么

| 文件 | 说明 |
|---|---|
| `CHANGELOG.md` | 从 Cowork 会话记录重建的变更历史，覆盖 2026-07-29 至 08-17 的五次运行 |

`CHANGELOG.md` 是审计轨迹的**替代品**，不是原始 git 历史。提交哈希保留，
但对应的 diff 已不存在，无法逐行核验；数值转录自当时的运行汇报，未回溯原始出处二次核对。
详见该文件开头的说明。

## 待办

1. **恢复 `index.html`** —— 途径：Wayback Machine 存档，或任何本地/浏览器缓存副本。
2. **重建 `data/auto.json` 与 GitHub Actions 管线** —— 原管线每工作日抓 FRED，
   自动刷新核心 PCE 与单 A OAS 两条。workflow 文件不在 Pages 产物中，存档也抓不到，需要重写。
3. **恢复后接续轨迹** —— 以 `CHANGELOG.md` 为第一个提交，后续更新照旧写进提交历史。
4. **补 08-15 的缺口** —— 该日定时运行无完成记录，原因不明，未作填补。

## 面板的机制（备忘）

监测条件分三层，各有独立门槛：

| 层 | 含义 | 门槛 | 最后已知状态 |
|---|---|---|---|
| 环境 | 宏观环境 | 4 | 6/7 已断 |
| 结构 | 金融结构脆弱度 | 3 | 3/7 已断 |
| 引爆 | 触发事件 | 1 | 0/6 |

共 20 条（截至 2026-08-03）。判定自 07-29 至 08-17 未曾翻转。

阈值只接受三类依据：**市场惯例、机械后果、物理约束**。
禁止把线画在数据后面——给不出可信阈值的先进「候选」（有数没线，不计分）。

## 关于本次迁移

新仓库建在账号 `woowoeth` 下。原 `ourword-ai` 账号的封禁申诉与本仓库无关，
若申诉成功，原仓库的完整历史仍是更权威的记录。
